## 1. 前端共用控件

- [x] 1.1 抽取日期网格并统一月份导航、日格、边界与键盘操作；用组件测试验证跨月、上下界和选中行为。
- [x] 1.2 实现日期及时间输入弹层，支持直接输入、Escape、焦点返回及视口避让；用组件测试验证。

## 2. 前端场景接入

- [x] 2.1 将长期周期的三个日期字段和日历移动日期网格接入共用控件；用现有与新增交互测试验证范围及提交值。
- [x] 2.2 将提醒创建/修改、提醒设置和日程编辑接入共用控件；用现有与新增交互测试验证本地时间及保存值。

## 3. 验收

- [x] 3.1 运行相关测试、前端构建和 OpenSpec 严格校验，确认无原生日期/时间输入残留；记录结果。
- [x] 3.2 在实际界面检查中英文、双主题及窄宽度下日期/时间弹层的外观与可操作性；清理临时构建产物。

验收：前端 69 文件、589 项测试通过；`npm run build`、`openspec validate unify-date-time-pickers --strict`、`git diff --check` 通过。应用源码已无原生 `date`、`time`、`datetime-local` 或 `month` 输入；浏览器实际预览检查了中英文、白底/灰底、390px 窄窗口和长期周期弹窗内的日历定位与 Escape 焦点返回。临时预览文件和服务已移除，构建目录已清理。
