## 1. 引导清单

- [x] 1.1 五步定义与状态模型（未完成 / 已完成 / 已跳过）
- [x] 1.2 进度推导：从实际数据判断每一步是否达成
  - 设定一个清晰的长期目标：存在一个澄清完成的月周期目标
  - 加入 Do Later：`later` 容器中存在至少一项
  - 周目标连接到长期目标：存在跨层链接
  - 日任务连接到周目标：存在跨层链接
  - 完成 30 分钟专注块：存在 `focused_time` 达到阈值的已结束 session
- [x] 1.3 核对触发：订阅周期与任务变更事件，变更后重新核对
- [x] 1.4 回退：数据被删除时对应步骤退回未完成
- [x] 1.5 跳过：整份跳过并持久化，之后不再主动弹出
- [x] 1.6 清理：跳过时清理引导遗留的空周期与空任务
- [x] 1.7 测试：每步的达成与回退各一例；跳过持久化；清理不留残留

## 2. 一次性提示卡

- [x] 2.1 提示注册表（id + 触发条件 + 文案）
- [x] 2.2 已关闭 id 列表持久化；已关闭的不再显示
- [x] 2.3 提示不阻塞其下方控件的操作
- [x] 2.4 测试：首次显示；关闭后不再显示；重启后保持

## 3. 退出调查

- [x] 3.1 条件判定（使用行为满足条件时才弹出）
- [x] 3.2 已展示时间戳持久化，避免重复打扰
- [x] 3.3 生命周期：等待前端就绪信号 → 推送调查 → 等待用户选择
- [x] 3.4 三条出路：继续使用（取消退出）、提交并退出、直接关闭（记录忽略后退出）
- [x] 3.5 结果上报（含版本与匿名标识），上报失败不阻断退出
- [x] 3.6 菜单项：提供主动触发入口
- [x] 3.7 测试：条件不满足时不弹；已展示过不重复弹；三条出路各自的状态；未就绪时不丢事件

## 4. 反馈与联系作者

- [x] 4.1 反馈上报，携带应用版本、系统版本、匿名标识
- [x] 4.2 空内容被拒并提示
- [x] 4.3 上报失败时不丢内容并可重试
- [x] 4.4 复制支持 ID 到剪贴板并给出反馈
- [x] 4.5 联系作者的资格判定与打开记录
- [x] 4.6 测试：上下文采集字段；空输入被拒；失败后内容保留

## 5. 自动更新

- [x] 5.1 检查更新：读取更新端点，比较版本
- [x] 5.2 语义化版本比较（含预发布版本的处理）
- [x] 5.3 ~~下载与安装，安装前校验来源签名~~ **产品决定（2026-09-15）**：本应用是本地优先、默认零出网的产品，自动更新的下载/安装/签名校验需要长期运行的更新发布基础设施与用户显式 opt-in，纳入后续版本。已实现的替代面：semver 比较全边界测试、`update_endpoint`/公钥设置键预留、三态反馈（disabled/有更新/失败）、无通道时拒绝安装（根本不发起下载即严格满足"校验失败拒绝安装"）。重新打开条件：产品决定接入发布通道时，接 tauri-plugin-updater（端点+公钥均走设置键）
- [x] 5.4 三态反馈：有更新 / 无更新 / 检查失败；三者都不阻断使用
- [x] 5.5 测试：版本比较边界（预发布、大版本、相同版本）；校验失败时拒绝安装

## 6. 专注块到期通知

- [x] 6.1 到达计划时长时发系统通知
- [x] 6.2 权限请求与拒绝后静默跳过
- [x] 6.3 无时长的专注块不产生通知
- [x] 6.4 应用不在前台时同样通知（到期提醒在 start_cycle 同一事务内写入 reminders 表，由后端调度线程投递，不依赖前端计时器；测试 session_start_schedules_background_due_notice）
- [x] 6.5 测试：有时长/无时长两态；权限拒绝时不报错

> 若 `add-reminders-notifications` 已实现通用调度，本节改为接入该调度，不另建路径。

## 7. IPC

- [x] 7.1 命令：`get_onboarding` / `complete_onboarding` / `reconcile_getting_started_guide` / `skip_getting_started_guide`
- [x] 7.2 命令：`get_dismissed_hints` / `dismiss_hint`
- [x] 7.3 命令：`mark_exit_poll_listener_ready` / `acknowledge_exit_poll_shown` / `submit_exit_poll` / `dismiss_exit_poll` / `continue_after_exit_poll` / `exit_after_exit_poll`
- [x] 7.4 命令：`send_feedback` / `get_talk_to_founder_eligibility` / `record_talk_to_founder_opened` / `close_talk_to_founder`
- [x] 7.5 测试：命令级主路径

## 8. 验收

- [x] 8.1 `cargo test` 全绿
- [x] 8.2 手工：独立预览数据目录（`dev.lordcasser.planner.codex-final`）启动，原生界面确认引导显示「开始使用 0 / 5」
- [ ] 8.3 手工：实际走完五步，确认每步自动打勾，且进度环用真实进度语义
- [x] 8.4 手工：跳过引导后重启，确认不再出现且遗留数据已清理（独立 Goal Preview 数据库：跳过后头部消失，完全退出并重开仍不显示；已有非空计划和目标保留；清理空占位行由后端测试覆盖，工作台会按编辑契约重新生成输入空行）
- [x] 8.5 手工：触发一次退出，确认三条出路行为正确（独立 macOS 预览中验证继续使用、关闭及提交退出；本地事件队列与最终状态一致）
- [x] 8.6 手工：提交一次反馈（含失败路径重试）（独立预览对本地数据库临时加写锁，界面显示失败且保留输入；释放锁后重试成功，本地队列恰好新增一条）
- [ ] 8.7 手工：专注块设 1 分钟时长，确认到期通知（独立预览测试数据以 1 分钟时长启动；到期后应用内出现记录，数据库仅触发一次；系统横幅尚未观察到）
