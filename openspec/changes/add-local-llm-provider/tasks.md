## 1. 供应商抽象

- [ ] 1.1 扩展供应商枚举：`hyperfocus | openrouter | local`；设置读取与序列化同步更新
- [ ] 1.2 `ai/llm/openai_compat.rs`：实现 OpenAI 兼容客户端（`/v1/chat/completions`、`/v1/models`）
- [ ] 1.3 `ai/llm` 的 `LLMProvider` 实现新增本地分支；`resolved_client` 按设置解析
- [ ] 1.4 超时分层：探测超时（默认 2s）与生成超时（默认 120s，可配置）

## 2. 探测与模型发现

- [ ] 2.1 默认端点候选列表（Ollama `11434`、LM Studio `1234`、llama.cpp `8080` 等），并发探测
- [ ] 2.2 响应结构校验：`/v1/models` 返回体必须符合兼容格式，否则判为不可用
- [ ] 2.3 一次最小对话请求做二次确认，避免把别的 HTTP 服务误认为模型服务
- [ ] 2.4 手工端点输入与同样的校验流程
- [ ] 2.5 测试：模拟兼容服务、模拟占用端口的非模型服务、全部不可用

## 3. 能力探测

- [ ] 3.1 发一次带工具定义的请求，判定模型是否返回结构化工具调用
- [ ] 3.2 结果写入 `local_supports_tools` 并缓存；切换模型时失效
- [ ] 3.3 探测失败按「不支持」处理，并提供重新探测入口
- [ ] 3.4 测试：支持 / 不支持 / 返回不明确三种响应

## 4. agent 集成

- [ ] 4.1 回合执行支持流式文本：增量推送到前端事件，回合结束后一次性落库
- [ ] 4.2 不支持工具调用时，工具定义不下发，回合标记为对话模式
- [ ] 4.3 上下文裁剪：超窗口时优先保留当前周期与当前任务，丢弃历史复盘与远端周期
- [ ] 4.4 超时与连接错误映射为可读的 `AgentError`，不污染既有数据
- [ ] 4.5 测试：流式顺序与落库序号一致；对话模式下不产生任何 `agent_proposal` 行；超时后会话可继续

## 5. 授权交互

- [ ] 5.1 本地供应商路径不请求托管 AI token
- [ ] 5.2 试用结束的判定不再阻断本地供应商
- [ ] 5.3 测试：试用过期 + 本地供应商下 agent 全程可用且无出网请求（用假端点断言零外部调用）

## 6. 设置界面

- [ ] 6.1 AI 访问区新增第三项「本机运行时」，含探测状态、模型下拉、连接测试
- [ ] 6.2 不支持工具调用时的显式说明文案
- [ ] 6.3 手工端点输入与校验错误反馈
- [ ] 6.4 测试：探测中/可用/不可用/不支持工具四种界面状态

## 7. 验收

- [ ] 7.1 `cargo test` 与 `npm run test` 通过
- [ ] 7.2 手工：启动本地运行时 → 探测 → 选模型 → 跑一轮澄清对话 → 产生待确认改动并 Keep
- [ ] 7.3 手工：停掉本地运行时，确认错误可读且应用不崩、数据不变
- [ ] 7.4 手工：断网 + 本地供应商，确认 agent 可用
