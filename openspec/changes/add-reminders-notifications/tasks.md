## 1. 数据

- [x] 1.1 迁移：`reminders(id, target_kind, target_id, fire_at, quiet_ok, fired_at, dismissed_at, created_at)`，`target_kind IN ('task','session','day','cycle')`
- [x] 1.2 唯一约束：`(target_kind, target_id, fire_at)` 对未处理提醒唯一
- [x] 1.3 目标删除时清理：由 service 层在删除路径上处理（SQLite 无法对多态外键建约束，需测试兜住）
- [x] 1.4 设置项：`quiet_hours`、`daily_plan_time`
- [x] 1.5 测试：重复设定复用既有记录；删除目标后提醒被清理

## 2. 调度器

- [x] 2.1 进程内调度器：下一次触发时间的单定时器 + 到期队列，不用每条一个定时器
- [x] 2.2 对账入口：应用唤醒、窗口聚焦、设置变更时按 `fire_at <= now AND fired_at IS NULL` 重查
- [x] 2.3 到期处理顺序：按 `fire_at` 升序；同一批内先去重再投递
- [x] 2.4 目标已完成 → 静默标记；目标已删除 → 清理
- [x] 2.5 测试：定时器漂移场景（人为把 `fire_at` 设到过去）；批量到期顺序

## 3. 投递与降级

- [x] 3.1 通知权限请求与状态缓存
- [x] 3.2 免打扰判定：`quiet_ok` 与时段相交时抑制系统通知但仍记录
- [x] 3.3 权限被拒或投递失败 → 应用内提示 + 状态记录
- [x] 3.4 专注块到期改由本调度器投递，移除原路径（对外行为不变）
- [x] 3.5 测试：权限授予/拒绝两态；免打扰内外的差异；专注块通知不重复

## 4. 启动补偿

- [x] 4.1 启动时查询未运行期间到期的提醒
- [x] 4.2 聚合摘要：总数 + 最多三条明细 + 可展开
- [x] 4.3 关闭摘要 → 全部标记已处理
- [x] 4.4 测试：无 / 少 / 多三种数量；摘要关闭后不再出现

## 5. 界面

- [x] 5.1 任务与专注块上的提醒设置入口（时间选择）
- [x] 5.2 日计划提醒设置（沿用 `daily_plan_time`）
- [x] 5.3 提醒列表（按时间排序，可改可删）
- [x] 5.4 免打扰设置与权限状态展示
- [x] 5.5 启动摘要组件
- [x] 5.6 测试：列表排序；改时间后旧时间失效；权限被拒状态展示

## 6. 验收

- [x] 6.1 `cargo test` 与 `npm run test` 通过
- [ ] 6.2 手工：设一个 1 分钟后的提醒，确认按时弹出；完成该任务后确认不再弹
- [x] 6.3 手工：设一个提醒 → 完全退出应用 → 跨过触发时间 → 重启确认出现错过摘要（独立 Goal Preview：18:07 的周任务提醒在应用退出期间到期，重启显示 1 条错过提醒，展开显示任务与时间；修正调度线程先于摘要收集的启动竞态，并补服务回归测试）
- [ ] 6.4 手工：开启免打扰，确认该时段内不弹系统通知但应用内可见
- [ ] 6.5 手工：在系统设置里关闭通知权限，确认应用内提示照常且设置页给出指引
