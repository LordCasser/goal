## 1. 数据

- [x] 1.1 为任务新增纯文本备注列、默认值和 Rust／TypeScript 数据契约；用迁移测试验证旧任务读出空备注。
- [x] 1.2 增加自动导入与稍后数量两个持久化设置及 IPC 入口；用设置读写测试验证默认值与重启后保留。
- [x] 1.3 核对复制、移动和 AI 提议各写入路径的备注语义；用后端测试验证复制为空、移动保留、提议不覆盖。
- [x] 1.4 运行数据层迁移与任务仓储测试，确认既有数据和外键约束可用。

## 2. 后端

- [x] 2.1 实现同标题事务按标题与直接上级目标身份分组、按日期完成分段和日数计算的按需查询；用测试覆盖不同目标、间隔日、同日重复及完成后重启。
- [x] 2.2 实现显式创建新周／日计划时的相邻周期自动导入与来源血缘，保证事务性与按组合键防重；用测试覆盖设置关闭、跨周、重试和失败回滚。
- [x] 2.3 为事项备注读写和日事务详情提供命令，并检查锁定计划和提议状态；用服务测试验证只读与写入失败语义。
- [x] 2.4 运行相关 Rust 服务测试，并在完成后清理本次 Cargo 构建残留。
- [x] 2.5 Later 事项恢复到计划时，将其持久顺序放在现有空输入行之前并接续现有事项；保留其他插入路径原有的持久排序语义。

## 3. 前端

- [x] 3.1 实现任务双击详情与逐条备注编辑，日事项显示事务日期记录；用交互测试验证单击标题、双击详情、只读和保存失败。
- [x] 3.2 接入两个设置开关及左上角稍后数字；用前端测试验证数量口径、0、99+、关闭和缓存失效。
- [x] 3.3 保留任务行首把手用于同一计划列表内排序，不增加右侧排序按钮；补日期选择改期和待排专注块首次时间编辑，复用已有服务并验证冲突处理、失败保留和键盘入口。
- [x] 3.4 移除跨列表／跨周期移动、目标关联、日历和时间轴的业务拖拽处理及失效提示；保留同一计划列表内排序把手。用 UI 测试验证仅同列表拖动会排序、其他拖动不写数据，窗口标题栏仍保留平台拖动标记。
- [x] 3.5 实现应用自有右键菜单与键盘触发、文本编辑剪贴板操作；用交互测试验证定位、焦点、Escape、选区和动作一致性。
- [x] 3.6 更新中英语文案并运行前端构建与相关测试，核对双主题、窄窗口和减少动态效果。
- [x] 3.7 计划编辑器始终将空输入行显示在所有可见事项之后，并保持现有行内输入与标题编辑交互。
- [x] 3.8 单项计划中的非空可编辑事项也保留行首把手，默认隐藏、悬停或键盘聚焦时显示；空输入行与锁定提议不显示把手。
- [x] 3.9 双击标题打开详情时收起原生文字选区，保存或关闭后焦点仍返回标题；计划与 Later 行均覆盖回归测试。

## 4. 验收与规范

- [x] 4.1 同步 `docs/design.md`、`docs/desktop-build.md`、主规范和活跃 `add-calendar-time-view` 的冲突拖拽描述；明确计划空输入行始终显示在最后，Later 恢复项持久排序在其前，并保留行内输入交互。
- [x] 4.2 运行 `openspec validate refine-task-interactions-and-continuity --strict`、Rust 与前端相关测试，记录仍受平台条件限制的验收项。
- [ ] 4.3 手工检查 macOS、Windows、Linux 的右键剪贴板、键盘焦点和窗口拖动；可用平台逐项记录结果，不可用平台记录待验收原因。
