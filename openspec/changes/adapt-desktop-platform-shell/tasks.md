## 1. 数据边界与基线

- [x] 1.1 核对当前窗口、生命周期、配置和凭据入口，记录实施前源码版本/工作树状态及三平台契约；以记录明确 app identifier、窗口 label、版本、数据路径、凭据服务名不变，不新增业务实体或数据库迁移。
- [x] 1.2 用专门测试数据建立包含已保存计划与 Coach 待确认改动的关闭/重开基线；验证重开后状态符合既有持久化契约，并记录截图或测试结果供最终回归比较。

## 2. 后端与编译隔离

- [x] 2.1 拆分公共 Tauri 配置与 macOS/Windows/Linux 覆盖文件，按 design D5 保留完整窗口数组字段、正确目标格式和图标；用合并配置检查确认三个目标均保留 main、1280×800、960×600 和相同应用身份，macOS 专属属性不进入其他平台。
- [x] 2.2 按 design D4 实现构建目标归一、Vite 常量与 `desktop-build.json`，在构建及打包 hooks 检查平台/版本；验证 darwin、Windows、Linux、独立 Web 路径，并故意提供缺失目标、未知目标、错平台/错版本/缺失标记，确认原生打包失败而非静默退回 Web。
- [x] 2.3 将 keyring 后端与 Windows API 依赖改为 Cargo target dependencies，隔离原生 imports、模块和命令注册；用各目标 `cargo tree --target <triple> -e normal,build,features` 检查正确后端及 SDK 依赖，并确认未升级无关依赖或启用分发用 mock 凭据。
- [ ] 2.4 在当前锁定框架上建立仅 Windows 编译的最小外壳适配与测试控件，先验证原生最大化命中、Snap 菜单、拖动、边缘缩放和系统菜单；在 Windows 11 原生窗口留存可复现结果后才进入视觉细化，浏览器模拟不算通过。
- [ ] 2.5 完成区域几何校验、DPI/客户区坐标转换、窗口消息链和状态订阅，处理 WebView2 子窗口的消息边界；验证越界/重叠/陈旧区域拒绝、跨 DPI 命中一致及一次点击仅执行一次动作，窗口销毁后无残留回调。
- [ ] 2.6 完成 Windows 外壳就绪握手与最长 3 秒失败恢复，失败使用系统装饰、普通工具栏和原有日志；故障注入缺少前端就绪、原生初始化错误和非法区域更新，实测窗口能够显示、移动、缩放、关闭且无双套控件。
- [x] 2.7 按平台收敛 capability，仅本地主窗口可调用 Windows 所需控制及外壳 IPC，公共保留既有 dialog/opener；验证 Linux/Web 不请求自绘窗口操作、窗口 IPC 不接受任意句柄或消息号、Coach 工具目录没有外壳管理入口。
- [x] 2.8 添加仅手动触发的三平台构建验证 workflow，锁定工具链、列出原生依赖、分离缓存和产物，保存构建记录与校验和；检查 workflow 无自动 tag/Release/签名上传行为，且每个平台重新构建前端、不复用另一平台 dist。
- [x] 2.9 运行构建契约负向用例、目标依赖检查与受影响 Rust 测试；保留命令、结果和失败原因，确认平台差异止于外壳/凭据/打包边界，没有进入计划领域逻辑。实际安装包结果由第 4 组分别验收。

## 3. 前端与窗口交互

- [x] 3.1 增加小型平台模块，消费构建常量并统一主修饰键匹配与提示；用单元测试覆盖 macOS/Windows/Linux/Web、错误修饰键、IME、repeat，确认原生目标不依赖 UA 或用户设置，Web 不启用系统控制与原生拖拽。
- [ ] 3.2 调整共享 WindowBar：macOS 52px、Windows/Linux/Web 44px；Windows 为右侧三个 46×44px 控件留区，保留分组、设置间隔和至少 64px 空白拖动区；在 1280px、1100px 阈值两侧及 960px 下检查两个主题，无重叠、双层栏或红绿灯空槽。
- [ ] 3.3 将 Windows 控件与第 2 组原生适配接通，提供 12px 清晰图标、hover/pressed/focus/inactive/高对比度及减少动态效果状态，真实窗口状态驱动最大化/还原名称与图标；测试鼠标、键盘动作各执行一次，关闭走既有生命周期，系统降级时自定义控件消失。
- [ ] 3.4 实现 macOS 全屏安全区与实际原生状态同步，补齐窗口监听器清理；通过原生全屏往返及系统控制临时出现验证 104px/16px 状态不重叠、不残留空槽，窗口按钮出现前恢复必要安全区。
- [x] 3.5 替换 Later 与 Coach 中硬编码快捷键，统一文案和事件守卫；验证 macOS Command 与 Windows/Linux Control 的正确触发，模态阻挡、隐藏/inert/busy Coach、IME、长按和无对应候选时不误操作或重复发送。
- [ ] 3.6 校正客户区 flex/滚动边界与中文字体回退，保留计划面、侧栏和公共组件尺寸；在 960×600 双侧栏、设置模型表单、Coach 表格和长文案下检查关闭/提交入口可达，主题与 DPI 变化不把整个窗口撑宽。
- [ ] 3.7 运行受影响组件测试与现有前端回归，补充原生状态变化、初始化失败、窄工具栏分组及输入守卫的有意义测试；实际查看两主题的四种外壳展示并保存对照图，明确浏览器截图仅作为布局证据。

## 4. 原生验收与交付

- [ ] 4.1 在 macOS 原生环境分别生成 arm64 与 x86_64 `.app`/`.dmg`，检查每个产物的对应架构，安装启动并验证红绿灯、拖拽、全屏往返、快捷键、双主题及重新激活后的绘制；保存版本/架构/截图/日志，原有重绘问题如复现需单独记录。
- [ ] 4.2 在 Windows MSVC 环境分别生成 amd64 与 arm64 NSIS 安装包并实际安装启动；检查单行融合栏、按钮三态/最大化还原、右击与 Alt+Space 菜单、Alt+F4、边角缩放及 Windows 11 原生 Snap，分别记录菜单出现与可容纳分区贴靠结果，不能用系统装饰降级替代通过。
- [ ] 4.3 在 Windows 实测 100/125/150/200% DPI、跨显示器、最大化往返、两个主题、键盘/Narrator/高对比度；核对视觉与命中区域一致、窗口和业务按钮不会互相误触，有触控/笔设备时补充对应拖动证据，缺少设备明确记未验证。
- [ ] 4.4 在 Linux 原生环境分别构建 amd64 与 arm64 AppImage/deb，分别检查安装/启动和依赖；在 GNOME/KDE 的真实会话中覆盖 X11/Wayland、窗口按钮偏好、正常/高 DPI 与两主题，留存具体发行版/桌面/会话及结果，未具备的组合保持未验证。
- [ ] 4.5 在三个平台用专门测试数据回归临时日任务完成、Workspace/Calendar 内容联动、Coach 预览确认/拒绝及关闭重开、设置关闭；验证文件保存/打开、既有通知流程和凭据重启读回，Linux 凭据服务不可用时本地计划仍可用且无明文回退，完成后只清理测试数据。
- [x] 4.6 整理平台验收记录和构建说明，实施验证后同步根 `design.md`、`docs/architecture.md` 及本 change 对应主规范；确认最低系统版本、签名发布、窄 Snap 分区与原有重绘债务分别列出，未测试的平台不标记完成。
- [x] 4.7 执行 `openspec validate adapt-desktop-platform-shell --strict`，逐项对照场景与证据核查完成状态；确认提交范围仅含本变更及必要文档，没有版本发布或业务模型改动。保存所需安装包/日志后按实际 target 目录执行 `cargo clean`，核对释放空间并保留验收产物。


## 本轮执行结果（2026-09-15）

实现与证据见 `analysis/reports/44-desktop-platform-shell-2026-09-15.md`。已完成 12/25 项；未勾选项包含真实 Windows/Linux 控制、安装、DPI/辅助技术、macOS universal、精确视口与四平台视觉证据。对应代码与测试基础已落地，但没有用 macOS 或组件模拟替代原生验收。本 change 保持 active，不归档。


## 5. v0.1.1 跨平台复核补充（2026-09-16）

- [x] 5.1 排查 Windows 路径、非 ASCII/空格目录、原子保存与命令调用，修复实际问题并补对应测试。
- [x] 5.2 统一应用内 Select，迁移五处设置选择器；验证锚点、禁用、键盘与嵌套 Escape，并实看中英文原生窗口。
- [x] 5.3 修复浮层表单方向键被菜单截获、约束浮层客户区尺寸，验证输入与菜单各自的键盘语义。
- [x] 5.4 复核凭据、通知、窗口、字体与 DPI 的隔离边界；更新架构和审计记录，区分自动测试与原生平台未验证项。

2026-09-16：按用户后续发布要求，4.1–4.4 的产物改为三平台双架构独立包；不再要求 macOS universal。原生交互验收保持未完成。
