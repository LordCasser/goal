## 1. 查询

- [ ] 1.1 `calendar::range_query`：给定日期区间批量返回日周期及其专注块（避免按天 N 次查询）
- [ ] 1.2 月网格需要的前后填充日期一并返回，保证网格不出现空洞
- [ ] 1.3 测试：跨月区间、区间内无任何日周期、单日多个专注块

## 2. 跨日期移动

- [ ] 2.1 `move_day_cycle(cycle_id, target_date, strategy)`，`strategy ∈ ('move','merge','swap')`
- [ ] 2.2 处理 `calendar_key` 唯一索引冲突：按 strategy 在单个事务内完成，冲突时不产生半完成状态
- [ ] 2.3 合并策略：把源条目并入目标日周期并保持 `position` 顺序；源日周期删除
- [ ] 2.4 交换策略：两天的 `calendar_key` 与 `starts_on` 在一个事务内互换（需先置空再写，绕过唯一索引）
- [ ] 2.5 沿用既有移动守卫：已启动专注块所在的日子不可被静默合并
- [ ] 2.6 测试：三种策略各一例；目标已存在且未指定策略时返回 `Conflict`；事务失败不留半成品

## 3. 时间轴排布

- [ ] 3.1 `set_session_schedule(session_id, starts_at, duration)`：写入开始时间与时长
- [ ] 3.2 区间重叠检测作为查询结果返回（不修改数据）
- [ ] 3.3 测试：重叠区间、零时长、跨午夜区间（跨午夜按时长截断到当日并标注）

## 4. 时间预算

- [ ] 4.1 设置项 `daily_capacity_minutes`，未设置时不显示预算
- [ ] 4.2 当日已排时长聚合（包含未开始但已排的专注块）
- [ ] 4.3 测试：未设置、未超、超出的三种呈现

## 5. 界面

- [ ] 5.1 月网格与周网格组件，共用同一单元格渲染
- [ ] 5.2 格子摘要：专注块数量与完成进度
- [ ] 5.3 空格子的一键创建当日计划
- [ ] 5.4 拖拽移动（沿用工作台的乐观更新策略），冲突时弹出策略选择
- [ ] 5.5 单日时间轴 + 待排区，支持拖入排期
- [ ] 5.6 时间预算条
- [ ] 5.7 顶栏视图切换；`preferred_view` 记忆
- [ ] 5.8 测试：月/周网格渲染、空格子创建、预算三种状态、单点数据不渲染误导性图表

## 6. 验收

- [ ] 6.1 `cargo test` 与 `npm run test` 通过
- [ ] 6.2 手工：在日历上创建日计划 → 拖到空日期 → 拖到已有计划的日期并选择合并 → 确认工作台一致
- [ ] 6.3 手工：给专注块拖入时间轴并制造重叠，确认两者都保留
- [ ] 6.4 手工：切换视图后确认滚动与聚焦位置保持
