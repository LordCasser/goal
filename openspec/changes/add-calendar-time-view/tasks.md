## 1. 查询

- [x] 1.1 `calendar::range_query`：给定日期区间批量返回日周期及其专注块（避免按天 N 次查询）
- [x] 1.2 月网格需要的前后填充日期一并返回，保证网格不出现空洞
- [x] 1.3 测试：跨月区间、区间内无任何日周期、单日多个专注块

## 2. 跨日期移动

- [x] 2.1 `move_day_cycle(cycle_id, target_date, strategy)`，`strategy ∈ ('move','merge','swap')`
- [x] 2.2 处理 `calendar_key` 唯一索引冲突：按 strategy 在单个事务内完成，冲突时不产生半完成状态
- [x] 2.3 合并策略：把源条目并入目标日周期并保持 `position` 顺序；源日周期删除
- [x] 2.4 交换策略：两天的 `calendar_key` 与 `starts_on` 在一个事务内互换（需先置空再写，绕过唯一索引）
- [x] 2.5 沿用既有移动守卫：已启动专注块所在的日子不可被静默合并
- [x] 2.6 测试：三种策略各一例；目标已存在且未指定策略时返回 `Conflict`；事务失败不留半成品

## 3. 时间轴排布

- [x] 3.1 `set_session_schedule(session_id, starts_at, duration)`：写入开始时间与时长
- [x] 3.2 区间重叠检测作为查询结果返回（不修改数据）
- [x] 3.3 测试：重叠区间、零时长、跨午夜区间（跨午夜按时长截断到当日并标注）

## 4. 时间预算

- [x] 4.1 设置项 `daily_capacity_minutes`，未设置时不显示预算
- [x] 4.2 当日已排时长聚合（包含未开始但已排的专注块）
- [x] 4.3 测试：未设置、未超、超出的三种呈现

## 5. 界面

- [x] 5.1 月网格与周网格组件，共用同一单元格渲染
- [x] 5.2 格子摘要：专注块数量与完成进度
- [x] 5.3 空格子的一键创建当日计划
- [x] 5.4 通过日期卡片右键菜单和应用内月历改期，冲突时弹出策略选择，不使用日期格拖拽或常驻角按钮
- [x] 5.5 单日时间轴 + 待排区，通过时间编辑入口设置排期，不支持拖入排期
- [x] 5.6 时间预算条
- [x] 5.7 顶栏视图切换；`preferred_view` 记忆
- [x] 5.8 测试：月/周网格渲染、空格子创建、预算三种状态、单点数据不渲染误导性图表
- [x] 5.9 任务行拖动把手仅支持同一计划列表内排序，不增加独立调整顺序按钮；跨列表拖动不改变顺序或目标关联
- [x] 5.10 日期与任务完成比同排；右键菜单区分删除整日计划、删除专注块与清除排期，分别确认，后两者原子执行并检查内容快照
- [x] 5.11 日历中的目标高亮沿真实父链显示，悬停临时预览，点击再次取消；日、周、长期目标颜色反馈一致

## 6. 验收

- [x] 6.1 `cargo test` 与 `npm run test` 通过
- [ ] 6.2 手工：在日历上创建日计划 → 从日期卡片右键菜单移到空日期 → 移到已有计划的日期并选择合并 → 确认工作台一致
- [ ] 6.3 手工：通过时间编辑入口安排专注块并制造重叠，确认两者都保留
- [ ] 6.4 手工：切换视图后确认滚动与聚焦位置保持

## 7. 专注块编辑反馈（2026-09-15）

- [x] 7.1 时间浮层支持同时编辑开始时间和持续分钟数，显示结束时间，验证取消、非法值与保存失败保留草稿
- [x] 7.2 验证同一排期命令写入后专注块时长、时间轴、预算与重叠结果一致，并在 macOS App 实测保存与重开
