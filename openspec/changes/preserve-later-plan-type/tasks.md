## 1. 数据契约与迁移

- [x] 1.1 为 `tasks` 增加可空 `later_plan_type` 的迁移、值域及 Later/非 Later 一致性约束，补齐 Rust/TypeScript `Task`、查询、插入和更新的序列化；新建 Later 想法写入 `month`，旧 `NULL` 仍可读并按长期语义展示；用迁移测试和仓储测试验证合法值、非法值、旧数据兼容及非 Later 行只能为 `NULL`。

## 2. 后端移动与提升

- [x] 2.1 抽取事务内共享移动 helper，维护同周期子树的类型赋值/清理、Later 共享容器下的独立跨层级项、移出 Later 时仅针对移动根项外部 `parent_id` 的无效关联解除，并保持合法同周期/相邻层级关系；用 Rust 服务测试覆盖长期/周/日移入、子树、分别移入的周项与长期项及失败回滚。
- [x] 2.2 将 `promote_later_goal` 的 `target_cycle_id` 改为可选：长期继续支持单周期直达和多周期选择，周/日由后端按本地日期与 `week_start_day` 复用或原子创建目标周期；用 Rust 命令/服务测试验证空目标、重复请求、结束周期、唯一日历键竞争和不产生孤立周期。

## 3. Later 前端交互

- [x] 3.1 接入可选目标参数和 `later_plan_type` 展示，按类型显示“长期目标/周计划/日计划”小字及“安排到长期周期/安排到本周/安排到今天”按钮，保留长期候选菜单；请求期间禁用当前行、失败以内联显示、成功只失效相关查询且不弹 toast；用 `LaterPanel`/`LaterTaskRow` 的 Vitest 测试验证标签、调用参数、错误保留和刷新行为。

## 4. 集成验收

- [x] 4.1 以自动化集成测试覆盖迁移后的数据库与全新数据库中的直接新增、长期/周/日移入、同周期子树、不同层级独立 Later 项、已有/缺失周日周期、当前本地日期和周起始日、重复点击、失败回滚及再次移入类型重算；运行 `cargo test` 与 `npm test -- --run`（或仓库现有等价命令），再用 GUI 验证类型标签、三个安排入口、行内错误和无成功 toast 与用户指南一致。
