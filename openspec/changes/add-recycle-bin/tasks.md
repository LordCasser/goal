## 1. 数据

- [x] 1.1 审阅当前工作区的未提交回收站代码与迁移，确认 `trash_entries` 的约束、索引、元数据和快照范围符合设计；以新建数据库和既有数据迁移测试验证。
- [x] 1.2 完成删除影响的快照采集，覆盖跨计划子任务、关联专注块、提醒、预览原值、计划问题忽略与复盘处置；以删除前后数据库行集合比对测试验证无遗漏。
- [x] 1.3 完成原位恢复与冲突回滚，保留原 ID、父项、日期、顺序和状态；以 Later、长期目标、整棵计划以及缺失父项/日期冲突测试验证。
- [x] 1.4 验证快照随备份导出保留、迁移后可读取，并清理测试产生的 Rust 编译残留；以迁移与备份集成测试及磁盘用量检查验收。

## 2. 后端

- [x] 2.1 将所有用户可见目标、计划、单个专注块和批量专注块删除接入同一归档事务；确认同一次批量动作只生成一个条目，以服务测试验证。
- [x] 2.2 将已确认的 Coach 删除接入归档，保持待确认提议和 Revert 语义；以 Keep/Revert 集成测试验证回收站条目只在 Keep 后出现。
- [x] 2.3 完成列出、恢复、多选永久删除命令及失效事件；永久删除必须对陈旧或不存在的选择整体报错，以命令/服务测试验证没有部分提交。
- [x] 2.4 补齐专注时间、提醒、跨计划关联和复盘处置的恢复测试；修正当前 `trash` 测试中非法的同层级跨计划链接，运行 `cargo test --manifest-path src-tauri/Cargo.toml --test trash` 及相关删除测试验收。

## 3. 前端

- [x] 3.1 在 Later 旁提供回收站入口，让两者使用同一左侧抽屉并保持跨平台工具栏布局；以 `WindowBar` 与 `App` 组件测试验收。
- [x] 3.2 完成回收站列表的来源、删除时间、长标题省略号、逐项恢复、全选和逐项选择；以组件测试验证键盘、空状态和失败提示。
- [x] 3.3 完成永久删除确认与中英文文案，确认前不得写入，取消后保持选择；以组件测试验证多选、取消、成功和失败。
- [x] 3.4 接入 `trash:changed` 与现有计划/任务缓存失效，并运行 `npm run build` 和相关 Vitest 测试验收删除、恢复后的工作台、日历和 Later 刷新。

## 4. 验收

- [x] 4.1 在独立测试数据库手工走通 Later 目标、长期目标、周/日计划及跨计划子项的删除与原位恢复；检查回收站来源、顺序、颜色、备注和提醒。（macOS 独立预览中逐项验证；恢复后原 ID、父项、位置、红色、备注和提醒时间与删除前一致）
- [ ] 4.2 手工验证上级计划缺失与日期身份冲突时条目保留，以及多选永久删除前取消、确认后的不可恢复；记录界面结果。（独立 macOS 预览已验证两类恢复冲突均保留条目、多选确认前取消保留选择；尚未执行永久删除确认）
- [ ] 4.3 检查大计划快照的本地数据库大小、删除/恢复耗时和平台工具栏宽度；只对复现的问题做范围内修正。
- [x] 4.4 运行 OpenSpec 严格校验、Rust 相关测试与前端构建/测试，视觉检查最新桌面界面，并清理 Cargo 编译残留；以全部命令通过和可打开的预览验收。
