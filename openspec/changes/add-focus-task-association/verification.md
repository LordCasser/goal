# 验证记录

日期：2026-09-16。

- `CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 cargo test --lib --tests`：532 通过，7 个既有忽略项，0 失败。覆盖完整 Rust 单元与集成测试。
- 随后补充旧版数据库升级场景，运行 `CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 cargo test --test focus_task_association`：10 通过，0 失败。旧专注默认无关联且累计保留。
- `openspec validate add-focus-task-association --strict`：通过。
- `git diff --check`：通过。
- 前端关联与删除测试 11 项通过，添加专注块表单的任务选择已完成 GUI 核验；入口与默认无关联行为由主任务验证。
- 文案复查后单跑 `TaskDeletion.test.tsx`：6 通过；`i18n.test.tsx`：5 通过。删除文案使用“已开始”，包含已完成区块，不误称为正在运行。

专注关联测试覆盖：可选创建、异日/空行/提议/不存在任务拒绝、引用与类型约束、多块汇总、重复结束、删除单块及任务子树、旧确认失效、提醒清理、祖先累计和事件失效、单独跨日移动解除关联、整日移动/合并/交换保留关联并转移累计、重复实例不继承关联、Coach 确认删除、周期删除涉及另一周期下的任务与专注。

数据库迁移版本新增为 12；版本断言同步更新。未修改已存在迁移的 SQL。编辑态返回任务派生累计，但没有新增任务行专注入口或常驻统计组件。完整前端回归：60 个文件、437 项测试通过（`/tmp/goal-drum-all-tests.log`）；浏览器核验可选任务下拉默认不关联，选项仅包含当天真实任务。

既有历史数据若曾经在旧版本整体移动过含已完成专注的日，可能已有不准确的周期累计；本变更修正后续移动的累计转移，不主动重写旧数据。这项历史修复需独立审计。

原生 macOS 验收包也已检查：添加专注块的关联选项默认为“不关联任务”，仅列出当日 hello/Test 任务；取消表单，无验收数据写入。TypeScript、Vite、原生打包与固定签名验证通过。
