## 1. 数据

- [x] 1.1 追加可空专注任务关联及索引，验证旧数据默认为空、引用完整性与区块类型约束。

## 2. 后端

- [x] 2.1 创建区块校验同日正式任务，并返回关联；编辑工作区返回派生任务时长，验证无效输入与重复结束不计时。
- [x] 2.2 删除影响、确认令牌、提醒、祖先累计和失效通知包含关联区块，验证手工及提议确认删除与旧令牌拒绝。
- [x] 2.3 任务跨日移动解除关联，整日移动合并保留关联与累计，重复实例不继承关联；运行对应 Rust 回归测试。

## 3. 前端

- [x] 3.1 在添加专注块表单中可选关联同日任务，默认不关联，不新增任务行入口或常驻统计；通过前端测试及 GUI 验证选择与无关联创建。
- [x] 3.2 删除确认展示关联区块影响，验证叶任务存在专注时也确认，并完成相关前端测试；已开始计数准确包含完成态区块。

## 4. 验收

- [x] 4.1 通过 OpenSpec 严格校验与 Rust/前端相关回归测试，记录实际执行结果。
