## 1. 设备指纹

- [ ] 1.1 `entitlement/device.rs`：从平台硬件标识派生指纹（macOS 取 IOPlatformExpertDevice，其他平台留明确未实现）
- [ ] 1.2 固定盐 + 哈希；日志与错误信息中只出现哈希值，不出现原始标识
- [ ] 1.3 首次生成后持久化，后续启动复用
- [ ] 1.4 测试：同机器两次派生结果一致；不同盐得到不同值；原始标识不出现在序列化输出中

## 2. 授权令牌

- [ ] 2.1 `entitlement/token.rs`：令牌结构（版本、指纹、签发/过期时间、状态、试用到期时间）与编解码
- [ ] 2.2 `entitlement/verify.rs`：签名校验（非对称签名，公钥内置）+ 指纹比对 + 过期判断
- [ ] 2.3 状态语义：`trial` 有效 / `paid` 视为已授权 / `blocked` 视为拒绝
- [ ] 2.4 `entitlement/service.rs`：启动时用本地令牌快速判定，后台再刷新；刷新失败不阻断启动
- [ ] 2.5 离线容忍：本地令牌未过期时按本地判定运行，网络不可达不降级
- [ ] 2.6 测试：指纹不匹配被拒；过期被拒；离线启动可用；刷新失败后仍按本地令牌运行

## 3. 托管 AI 通道

- [ ] 3.1 向 worker 端点换取访问凭据；凭据带有效期并缓存
- [ ] 3.2 凭据请求失败的错误映射（网络 / 拒绝 / 额度用尽）
- [ ] 3.3 额度用尽时返回可恢复路径的提示文案（引导去设置连接自有供应商）
- [ ] 3.4 **不实现任何购买、升级、额度包入口**（规范明确禁止）
- [ ] 3.5 测试：凭据缓存命中；过期后重新换取；拒绝时的错误 code；额度用尽时的提示

## 4. 凭据存储

- [ ] 4.1 `credentials/mod.rs`：钥匙串读写封装（保存/读取/删除），键名固定
- [ ] 4.2 保证凭据不出现在数据库、配置文件、日志、错误信息中
- [ ] 4.3 读取失败与"未配置"区分开（前者是错误，后者是正常空状态）
- [ ] 4.4 测试：保存后可读；删除后读为空；序列化设置时不含凭据

## 5. OpenRouter PKCE 授权

- [ ] 5.1 `openrouter_oauth`：生成 code verifier 与 challenge
- [ ] 5.2 本地回环回调服务器，仅监听回环地址，随机端口
- [ ] 5.3 授权 URL 构造与回调参数解析（含 `state` 校验）
- [ ] 5.4 取消流程：用户取消后关闭服务器并释放端口
- [ ] 5.5 成功后把换取的凭据写入钥匙串
- [ ] 5.6 内置结果页（成功 / 失败 / 取消三态），无脚本注入面
- [ ] 5.7 测试：challenge 派生正确；state 不匹配被拒；取消后端口释放；回调解析

## 6. 语音管线

- [ ] 6.1 后端：换取一次性转写 token，返回 token 与有效期
- [ ] 6.2 前端 worklet：采集 48 kHz → 重采样 16 kHz、`linear16` 小端、80 ms（1280 采样）一帧
- [ ] 6.3 权限与设备错误区分：权限拒绝 / 无设备 / 打开失败，各自独立提示
- [ ] 6.4 不支持 AudioWorklet 的环境给出明确失败，不进入半可用状态
- [ ] 6.5 转写文本回填输入框，**不自动发送**
- [ ] 6.6 与授权联动：额度用尽且未配置自有供应商时，语音入口停用并给出文案；使用自有供应商时不尝试用其 LLM 凭据换语音
- [ ] 6.7 测试：帧长与采样率断言；四种失败路径的错误 code；额度用尽时的停用

## 7. IPC 与设置

- [ ] 7.1 命令：`get_entitlement_summary` / `get_entitlement_support_id` / `get_ai_settings` / `set_ai_provider` / `save_openrouter_api_key` / `remove_openrouter_api_key` / `connect_openrouter` / `cancel_openrouter` / `get_deepgram_transcription_token`
- [ ] 7.2 `get_entitlement_summary` 返回不含敏感信息的摘要（状态、剩余时长、支持 id）
- [ ] 7.3 测试：命令级主路径；摘要不含指纹原文

## 8. 验收

- [ ] 8.1 `cargo test` 全绿；网络相关测试用本地假服务器而非真实端点
- [ ] 8.2 手工：全新安装启动 → 自动获得内置额度 → AI 可用
- [ ] 8.3 手工：删除本地令牌后重启 → 重新签发
- [ ] 8.4 手工：走一次 OpenRouter PKCE 授权（或在中止后确认端口已释放）
- [ ] 8.5 手工：断网启动确认规划器可用，AI 给出可读错误
- [ ] 8.6 手工：语音听写一次，确认帧格式与回填行为
- [ ] 8.7 确认钥匙串之外（配置、数据库、日志）搜不到凭据明文
