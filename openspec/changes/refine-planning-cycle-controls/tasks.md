## 1. 数据与后端

- [x] 1.1 追加检查安排字段和数据库约束，扩展自定义日期创建与默认预设；测试非整周、日期非法、检查配置互斥及重载。
- [x] 1.2 实现递归删除影响预览及确认 token，清理后代提醒并刷新跨周期内容；测试取消边界、影响变化、后代级联与独立专注块保留。

## 2. 前端

- [x] 2.1 创建弹窗加入自定义日期和两种检查方式，保存后可查看安排；组件测试预览、草稿切换、错误和提交参数。
- [x] 2.2 修复未选中时长选项与挂载中语言切换，并收细关联主工作台横向滚动条；现有和新增定向测试 10 项通过。
- [x] 2.3 GUI 任务删除入口统一先读取影响，有后代时弹窗确认，周期删除显示专注块数量；测试取消、确认和影响刷新。
- [ ] 2.4 主工作台鼠标滚轮默认横向、鼠标主键点击实际内容区后纵向，点击标题或区域外恢复横向；保持内容区边界 containment、阻止边缘串动，保留横向触控板和缩放；测试事件路由并 GUI 验收。
  - 事件路由回归、真实浏览器点击和滚动验证、macOS 原生区域标识检查均已完成；Windows 物理鼠标实机验收仍待完成，不以浏览器输入验证替代实机结果。
- [x] 2.6 视觉细化：移除计划/专注块内容体的大面积内嵌蓝色边框，改为现有标题预留位置中的 14px 中性、不可交互内联上下 SVG 图标；不显示可见文字，但保留本地化无障碍标签（“上下滚动”/“Vertical scroll”）和解释滚动行为的原生 tooltip。仅对应内容体 `data-wheel-active="true"` 时显示，未激活时隐藏但保留空间，不新增背景、胶囊或新行且不产生布局位移。时间导航使用仅图标控件，不以点击、Tab 或焦点激活滚轮；日期鼓轮的悬停滚轮路由与新提交语义由 4.1–4.6 实现。
- [x] 2.7 周导航基线：保留真实周起始日期顺序、当前周内联身份与独立选中状态，并清理旧的重复当前周入口；新的固定高度日期鼓轮、动态窗口、悬停滚轮、逻辑 index 夹边、当前行平滑居中、延迟提交、帧级窗口更新和面板过渡由 4.1–4.6 替代，旧的导航滚动与点击激活细节不再作为当前契约。
- [x] 2.8 调整计划卡和专注块侧卡的高度与滚动：卡片按内容自然增长，最小约 360px，受工作台可用高度上限约束且不造成窗口级纵向溢出；不再始终拉伸到整屏。计划卡头部与底部工作量摘要固定，仅中部任务行在达到上限后滚动；专注块侧卡使用同样的有界卡片和独立滚动区，保留主键点击激活。补充多任务、短列表、达到高度上限和窗口尺寸变化的定向测试及 GUI 验收。
- [x] 2.5 按视觉反馈去除检查方式的通栏底色和拉伸间隔框，使用圆点单选与自然宽度的间隔行；复核中英、定期/单次状态并保留键盘和验证行为。

- [x] 2.9 日导航基线：保留真实日期边界、空日期显式创建和周日来源隔离；新的连续日期鼓轮、固定高度、悬停滚轮、逻辑 index 夹边、当前行平滑居中、500ms 防抖、程序对齐、帧级窗口更新和面板过渡由 4.1–4.6 实现，旧的导航细节不再作为当前契约。
- [x] 2.10 定向测试日导航日期边界、星期日起始、空日期不写入和来源隔离；日期鼓轮的跨年窗口、滚轮提交、联动及过期过渡验证由 4.3 和 4.6 覆盖。

## 3. 验收

- [x] 3.1 运行相关前后端测试、构建和 OpenSpec 严格校验；完成代码 review 与文档同步。
- [x] 3.2 用本地验收包验证中文/英文、自定义安排和删除确认，清理测试数据与可再生构建缓存。


## 4. 日期鼓轮导航

- [x] 4.1 将周、日导航实现为固定高度的连续日期鼓轮：跨年按真实日期动态扩展有界渲染窗口，不循环、不重复；悬停时独占纵向滚轮并抑制工作台横向滚动，计划卡和专注块继续使用鼠标主键点击后纵向滚动；按逻辑日期 index 计算当前周/今天自然位置，越界时夹到顶部/底部且不插入重复行；点击当前行平滑居中并打开对应计划/空态，浏览无计划周/日只显示空态，不自动创建数据。
- [x] 4.2 实现阻尼平滑吸附与来源隔离：滚轮中心候选须在至少 500ms wheel quiet 且运动 settled 后提交，中间候选不打开面板；周用户提交定位日鼓轮到保存起始日或周起始设置，日用户提交选择日期及所属周并对齐周鼓轮；程序定位不得生成新的用户提交或反馈循环，支持点击行、方向键、Page、Home（本周/今天）及减少动态效果。
- [x] 4.3 完成日期鼓轮实现与验证：覆盖跨年无限浏览无重复、历史周期同 slot 不丢失、悬停滚轮路由、500ms 防抖和运动结束、空态不写入、固定高度无布局跳动、当前行双边夹边/平滑居中、周日双向定位/中断隔离、键盘/点击和减少动态效果；完成浏览器及桌面 GUI 验收并同步相关文档。
- [x] 4.4 优化鼓轮帧更新与有界 DOM：缓存逻辑日期行及格式化标签，连续帧只更新已渲染行的 transform/opacity；仅在中心跨过当前行窗口边界时更新有界行集合，复用已有格式化标签，只格式化新增行且不引入额外批量缓冲层，保持固定视口、固定高度、同一 current 行夹边/回中和 500ms quiet + settled 提交，不在每帧重建 React 日期行或格式化日期。
- [x] 4.5 实现周/日面板原位内容过渡：目标面板的任务和专注块数据未全部就绪时保留旧内容布局且不可编辑；数据就绪后内容约 160ms ease-in 淡出至 0，卡片边框和背景保持不透明，目标内容原位约 260ms 缓启动缓停止淡入至 1；整卡不使用 translate/scale，卡片自然高度只在切换期间平缓插值并遵守可用高度上限；快速连续导航从当前显示状态接续，只保留最新目标和单一 live 内容树，减少动态效果时立即切换。
- [x] 4.6 验证滚动与面板切换性能：覆盖高速连续滚轮的有界 DOM 与稳定日期标签、无每帧 React 行重建、中心跨行窗口边界时的扩展与新增行格式化、复用已有标签且无额外批量缓冲层、500ms quiet + settled、当前行夹边/回中、旧内容保留与不可编辑、原位内容淡出淡入、卡片边框背景不透明、自然高度约 260ms 插值且不坍塌、无整卡 translate/scale、从当前显示状态接续、单一 live 内容树、过期过渡不提交、减少动态效果即时切换及目标面板任务和专注块数据就绪边界；关系绘制既有 batch query 保持不变；保留 Windows 物理鼠标实机验收未勾状态。
## 5. 规划列表删除退场

- [x] 5.1 实现任务、目标、稍后任务和专注块的数据确认后原位退场：仅在删除成功且对应查询结果真正移除条目后开始内容约 160ms 淡出与高度/占位约 260ms 收拢，分段重叠并让后续行连续让位，不使用弹跳或大位移；退出节点保持 inert、aria-hidden 且不可编辑、拖拽或聚焦。
- [x] 5.2 覆盖取消、失败、递归同批和快速独立删除：取消或失败不假消失，递归影响对象同批退场而不逐条排队；按删除操作身份隔离过期回调，不影响新加入或恢复的同 ID 条目，卸载时清理动画和回调，退场结束后才显示空态。
- [x] 5.3 将整张计划删除接入既有 PlanTransition：卡片位置平滑切换到下一个计划或空态，长期周期同样适用；不改删除 token、确认 Coach 流程、数据库契约或非规划类列表。
- [x] 5.4 验证真实查询移除边界、可访问性与键盘/拖拽锁定、递归同批退场、独立快速删除、恢复同 ID、组件卸载、空态时序、减少动态效果立即移除，以及计划卡切换的位置连续性。
