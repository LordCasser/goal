# 前端设计规范

本应用以一个连续工作台，把长期结果、本周计划、今天行动和专注执行接起来。前端应让用户始终知道三件事：正在看哪段时间、当前行动服务于什么、接下来可以做什么。

本文件整理自 **hyperfocus 0.15.0 原始 App** 和 **[官网中的 App 界面演示与功能配图](https://hyperfocus.in/)**（2026-09-14 核对），供后续应用前端重做使用。范围仅限 App 的工作台、组件和交互，官网页面自身的设计不在本文范围内。具体需求编号沿用 [UX 需求基线](analysis/reports/30-core-ux-requirements.md)，模块边界与数据所有权遵循 [架构文档](docs/architecture.md)。

当前视觉方案：[白底 / 灰底主题预览](analysis/design/window-and-surfaces.html)。用户确认两种视觉方向都保留为可选主题，具体规则见 3.4、4.1、4.4。

## 1. 依据与设计状态

| 标记 | 含义 | 使用方式 |
| --- | --- | --- |
| 原 App | 已实测界面或与 DMG 核对后的静态产物 | 作为行为、布局与视觉参考；静态路径不等于所有场景实测通过 |
| 官网 App 展示 | 官网里的规划板演示、App 功能配图 | 补充应用界面的视觉与交互表达；不作为真实保存、计时、AI 调用成功的证据 |
| 用户确认 | 用户明确加入的自有需求 | **UXR-033：点击目标或任务，持续展示关联关系**；**统一窗口栏，保留白底与灰底两个主题**（见 3.4、4.1、4.4，归入 UXR-032） |
| 设计基线 | 本文为首版提出的可调整方案 | 包括语义 token、尺寸、键盘路径、状态组合和面板收缩策略；不是对原产品的事实断言 |
| 待决策 | 涉及尚未选定的产品行为 | 沿用 Q 编号，不通过视觉设计提前决定数据语义 |

除明确标为观测值的内容外，下面的执行规则和数值均属于**设计基线**。这份文档不表示 35 条需求全部由用户逐项确认，也不自动替换存在冲突的旧 OpenSpec。

证据入口：

- [原 App 实测](analysis/reports/31-product-use-and-evidence.md)：O01～O24、截图和状态变化。
- [官网 App 展示复核](analysis/reports/33-website-ux-ui-review.md)：本文采用 W02～W06 的应用展示，保留演示与实测的区别。
- [原 App 视觉值](analysis/reports/design-system.md)：颜色、字体、尺寸、组件与动效的静态观测。
- [前端交互拆解](analysis/reports/40-frontend-ux-deconstruction.md)：状态与渲染路径；旧平台 API 推断须结合复核说明阅读。
- [待决策及架构债务](analysis/reports/32-rebuild-decisions-and-gaps.md)：Q01～Q12、A01～A04。

官网中采用的 App 展示：

| 展示 | 提取内容 | 对应章节 |
| --- | --- | --- |
| W02 交互规划板 | 时间导航、连续计划区、任务行、关系查看、助手消息层级 | 3、5、6、8 |
| W03 [目标关系配图](analysis/evidence/website-review-2026-09-14/connected-goals-ZoxmkM_E.png) | 归属色槽、跨列连线、任务组高亮 | 6 |
| W04 [反馈与助手配图](analysis/evidence/website-review-2026-09-14/second-opinion-CwaH4xDf.png) | 具体问题、处理与忽略入口、对话层级 | 8 |
| W05 [周日规划配图](analysis/evidence/website-review-2026-09-14/planning-rituals-jSG0Zn2d.png) | 计划标题、日期信息、列表与下一步行动 | 3、5 |
| W06 [专注执行配图](analysis/evidence/website-review-2026-09-14/focus-block-progress-Zpim63eo.png) | 运行标题、Stop、剩余时间、进度、行动清单 | 7 |

## 2. 设计原则

| 原则 | 界面如何体现 |
| --- | --- |
| 目标优先 | 长期目标、周计划、日计划是工作台主线；Later 提供快速收纳，不成为唯一工作入口 |
| 内容优先 | 主要空间用于阅读和编辑任务；任务不必打开详情页才能改一个标题 |
| 关系按需显露 | 平时使用轻量色标；关注时显示关系；点击后持续查看，不让全部连线常驻 |
| 允许计划不完整 | 普通事务可以不链接；可以先记再澄清；反馈不成为编辑和执行的门槛 |
| 控制权可见 | 创建前看日期，删除前看影响，AI 改动可比较、保留与撤销 |
| 低干扰但可操作 | 正文、操作状态和键盘焦点可读；不能靠过低透明度隐藏必要信息 |
| 窗口与内容统一 | 系统窗口控件、应用入口和工作台使用连续的顶部区域，统一底色、对齐与控件密度 |

App 的视觉语言是清晰正文、留白和有限的颜色强调。白底主题用连续表面和必要分隔建立层次；灰底主题用浅灰画布承托白色内容面。两者共用字体、内容位置和交互，服务于反复查看和调整计划。

## 3. 工作台布局

### 3.1 空间结构

```text
统一窗口栏：[系统窗口按钮] Later / 引导          助手与状态 / 设置
─────────────────────────────────────────────────────────────────
可选 Later 面板 │ 横向工作台                          │ 可选上下文面板
                │ 长期导航 + 长期计划                 │ 问题 / AI 会话
                │       周导航 + 周计划               │
                │             日导航 + [日计划 | 专注块] │
─────────────────────────────────────────────────────────────────
                 工作台内的待确认改动栏（有提议时显示）
```

上图表达区域归属，不代表三个计划区纵向堆叠。实际从左到右排列长期、周、日；专注块在所选日的工作区内，没有第四套日期导航。顶栏和辅助面板固定，工作台在剩余空间中浏览。

![原 App 的长期与周计划、色标和连线](/Users/lordcasser/workspace/projects/goal/analysis/evidence/ux-audit-2026-09-14/08-week-linked.jpg)

来源：原 App O08。截图只展示当前视口，不能把被裁到屏外的日计划解释为另一个页面。截图中的测试任务和窗口临时控件不属于重做设计；窗口栏采用 3.4 的自有方案。

### 3.2 区域职责

| 区域 | 内容 | 交互规则 |
| --- | --- | --- |
| 统一窗口栏 | 原生窗口按钮、Later、引导、助手及状态、设置 | 系统标题区与应用工具区视觉合并为一层；拖动空白区与按钮命中区分开，见 3.4 |
| 时间导航 | 长期日期范围、周编号、星期/日期、添加入口 | 选中项同时有文字与视觉状态；上层改变后，下层范围随之更新 |
| 计划正文 | 类型标题、日期/剩余时间、任务、问题入口 | 标题稳定，任务可连续输入；普通反馈不占据整块正文 |
| 日执行区 | 专注块列表、添加、投入汇总 | 始终跟随所选日；剩余时间、实际投入和计划预算分别表达 |
| Later | 同样的任务编辑器、用途提示、关闭 | 支持先记录再安排；不要求日期或归属选择，不另造三套收纳桶 |
| 右侧上下文面板 | 问题详情或 AI 会话 | 复用面板外壳，明确对象；输入区固定，内容独立滚动 |
| 待确认栏 | 待处理数量、查看、批量操作 | 在工作台内留出空间，不盖住最后一行；关闭 AI 侧栏不等于处理提议 |

### 3.3 尺寸与窗口适配

| 项目 | 原始依据 | 首版设计基线 |
| --- | --- | --- |
| 统一窗口栏 | 原 App 的应用顶栏为 40px；不作为重做约束 | 首版总高 52px，包含窗口控件与应用入口；实施时调整 `--app-header-height`，不再叠加一条独立应用工具栏 |
| 辅助面板 | Later 440px，右侧 424px | 默认统一 424px，减少尺寸变体；内容不足时通过折叠面板让位 |
| 单个计划内容面 | 原 App 随区域使用不同宽度；部分内容容器上限 672px | 先以 512px 为默认宽度，正文换行；不把 672px 当作每列的实测宽度 |
| 时间导航槽 | 原 App 放在对应计划左侧 | 初始 88px，间距 16px；日期长时允许换行或完整提示 |
| 日计划与执行 | 同一个日工作区内相邻 | 日计划与执行列表各一块可读内容面；二者共用日期选择 |
| 内容内边距 | 原 App 常见 16/24px | 主内容 24px，面板 16px，密集菜单 8～12px |
| 弹窗 | 按用途使用不同宽度 | 普通/删除约 420px；周期选择约 640px，窄时选项与时间线纵向排列 |

适配按可用空间处理，不按“必须在一屏看见四列”处理：

1. 计划列保留可读宽度，工作台允许横向滚动；长内容允许纵向滚动。侧栏内的消息和输入不跟着主板滚动。
2. 打开辅助面板后，保留选中周期、编辑位置及滚动上下文。两个辅助面板使主板不足以容纳一块可读计划面时，暂时折叠较早打开的面板，保留其输入和状态。
3. 关系另一端在屏外时，不强行缩小文字或突然移动光标；提供父/子条目名称和定位入口。关系查看不依赖两端同时在视口内。
4. 弹窗和面板设置可用高度上限，长内容滚动，主操作仍可到达。关闭后把焦点还给触发入口。

本轮按 macOS 桌面工作台设计，适配规则需通过真实窗口中的编辑与面板组合验证。

### 3.4 窗口栏：与工作台统一

**用户已确认原应用窗口栏需要重新设计，重点是更统一、更好看。** 下面的一体化布局、52px 高度和控件样式是为这一方向提出的设计基线，具体尺寸尚未通过实现验证。

窗口顶部把系统窗口按钮与应用入口放在同一水平区域，与当前主题的画布使用相同底色：白底主题为白色，灰底主题为浅灰色。保留 macOS 原生关闭、最小化、全屏按钮；工具按钮沿用正文的字体和图标，中央留白用于拖动。两种主题都采用统一窗口栏。

```text
[原生窗口按钮]   [Later] [引导·未完成]       可拖动留白       [助手·状态] [设置]
────────────────────────── 轻量底边界 ────────────────────────────────
Later / 时间导航 / 计划正文 / 上下文面板，均从窗口栏下方开始
```

| 部分 | 首版设计基线 |
| --- | --- |
| 整体表面 | 高 52px，与当前主题的工作台画布同色；底部仅保留对应主题的 1px 细线，形成连续顶部，颜色见 4.1 |
| 系统窗口按钮 | 保留原生外观与行为；左侧先预留约 104px 安全区，应用按钮从安全区右侧开始。最终按原生按钮实际位置校准，不用网页仿制按钮替换 |
| 对齐 | 工具按钮高 32px，图标 16px，文字 13/20px、字重 500；与窗口控件视觉中心对齐，同组间距 8px，右边距 16px |
| 左侧入口 | Later 常驻；引导只在尚未完成且未主动隐藏时出现。完成后的帮助入口归入设置 |
| 中央留白 | 保持连续可拖动空间；应用名保留为系统窗口标题，视觉上省略重复的品牌标题 |
| 右侧入口 | 助手入口合并简短可用状态，设置保持稳定位置；费用和错误详情在对应面板展开 |
| 按钮状态 | 默认透明底、清晰图标；hover 使用当前主题的局部状态色，已打开的面板入口用局部浅蓝底 `#eef5ff` 与深蓝文字 `#2f62a3`；键盘焦点有清楚轮廓，控件圆角 4px |
| 与面板衔接 | 窗口栏横跨窗口；Later、主板和右侧面板在其下方分区。侧栏开合不移动系统按钮，不改变窗口栏高度 |
| 窗口外框 | 外部圆角和窗口投影由系统管理；内部内容面仍沿用细边界，避免再套一层装饰外框 |

操作与窗口状态：

1. 只有指定空白区承担拖动。Later、助手、设置、引导和菜单区域可正常点击与键盘操作，拖窗不应吞掉这些动作。
2. 双击拖动区、最小化和全屏等行为遵循系统窗口习惯；退出全屏后恢复控件与内容位置。全屏时按系统实际控件区域重新处理安全区，避免按钮遮挡。
3. 窗口失去焦点时，系统按钮交给系统处理，应用入口适度降低强调；正文和当前计划不呈现禁用状态。
4. 窄窗口优先收起引导文案和助手的次要状态文字，保留可发现入口及控件名称；必要时将低频入口放入更多菜单，维持一行高度和可拖动空白。

验收时同时看静态外观与操作：顶部只有一条整合后的窗口栏；底色、字级、图标和垂直中心一致；系统按钮不与应用入口重叠；拖动、按钮点击、失焦、全屏、侧栏开合和窄窗口都保持连续布局。

#### 本轮视觉复核与预览

[双主题预览](analysis/design/window-and-surfaces.html) 可切换“白底”和“灰底”，记住本次主题选择，开合 Later / 助手侧栏，并点击示例任务查看直接关联。两者都是自有产品的主题；预览顶部的主题开关用于看稿，正式 App 的入口见 4.4。

2026-09-15 更新后的两个主题（详细查看可打开上方交互预览）：

| 白底 | 灰底 |
| --- | --- |
| ![白底主题](/Users/lordcasser/workspace/projects/goal/analysis/evidence/window-design-2026-09-14/white-workspace-preview.jpg) | ![灰底主题](/Users/lordcasser/workspace/projects/goal/analysis/evidence/window-design-2026-09-14/gray-workspace-preview.jpg) |

预览已在浏览器检查 1440px、1024px 和 390px 宽度：页面无横向溢出，桌面工作台在自己的区域内横向浏览；窄预览中的侧栏可完整显示。色彩切换、侧栏开合、点击关系、键盘 Enter / Escape 和焦点恢复已检查。白底上的主文字、次文字、提示文字对比度分别为 14.61、5.55、4.86；完成框边界为 3.39。窄预览仅便于看稿，不代表已经定义手机产品。

本轮已实际打开 1200×768 的 hyperfocus 窗口，但桌面控制通道断开，系统截图接口仍返回权限拒绝，尚未取得新的实时截图。当前视觉判断依据为本仓库已有的 O08/O11 实测截图和浏览器中的新方案预览。原生窗口按钮、拖动与全屏行为须在实现后回到真实 App 验证；HTML 中的系统按钮、编辑和计时仅作布局示意。

使用 [UI/UX Pro Max](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill/blob/7f69fed6a2717900085f1bc3b263721f8ba025e2/.claude/skills/ui-ux-pro-max/SKILL.md) 复核：采纳浅底深字、用留白建立层次、状态反馈局部化的原则。检索命中 `Contrast Readability` 与 `Minimalism & Swiss Style`；留白检索未得到贴合结果，按其通用规则和当前界面判断处理。技能中的营销页构图、移动端字号与横向滚动限制不直接套入桌面规划器。

## 4. 视觉规范

### 4.1 颜色与表面

原 App 的观测值包括 `#fbfbfb` 编辑面、`#f0f0f0` 关系画布、白色面板、`#1e1e1e` 强正文、暖色强调及多种关系色。重做保留用户确认的白底、灰底两个主题；[src/index.css](src/index.css) 的现有灰阶仍是骨架状态，实施时按用途映射或调整。

以下语义名称是设计用语；不要求为了命名额外建立一套庞大的 token 框架。

| 角色 | 白底 | 灰底 | 用途 |
| --- | --- | --- | --- |
| 工作台与窗口栏 | `#ffffff` | `#f6f6f6` | 同一主题下窗口栏与画布连续，导航间隙同色 |
| 内容表面 | `#ffffff` | `#ffffff` | 计划正文、面板、菜单、弹窗的阅读面 |
| 轻量表面 | `#fafbfc` | `#f0f0f0` | 小范围辅助说明 |
| 局部 hover | `#f5f7f8` | `#e8e8e8` | 只覆盖正在操作的按钮或条目 |
| 主文字 | `#25292f` | `#25292f` | 任务标题、当前问题、重要读数；保持实色 |
| 次文字 | `#616974` | `#616974` | 日期、说明、次要操作 |
| 提示文字 | `#6b727b` | `#666e78` | 灰底上略加深，保持可读，不叠加低透明度 |
| 轻边界 | `#edf0f2` | `#e2e2e2` | 装饰性分隔；不独立承担控件或选中识别 |
| 计划外边界 | 仅导航侧细线 | 白色内容面四边 `#e3e3e3` 细框 | 绘制在共同内容布局之外，切换时不改变文字位置 |
| 控件边界 | `#858c96` | `#858c96` | 完成框等有操作含义的边界，保持清楚 |
| 焦点与导航选中 | `#2f62a3` / 底 `#eef5ff` | 同白底 | 键盘轮廓、当前周期和已打开入口；不替代目标归属色 |
| 暖色强调 | `#e85c2a`；小字 `#bd4520` | 同白底 | 运行进度和少量强调 |
| 危险 | `#d92d20` / 底 `#fff0ee` | 同白底 | 删除确认、破坏性提议，必须带动作名称 |
| 待确认 | 柔和暖浅底 + 边线 + 文案 | 同一语义，分别校验对比度 | AI 提议；不能与运行状态仅靠色差区分 |

主按钮用深色底和白字，次按钮默认白色或透明底。暖色留给运行和少量强调；目标归属色独立表达关系。

白底主题减轻背景面积与围框；灰底主题保留浅灰画布和白色计划面。两者都把选中、hover 和提议底色限制在有关条目内，辅助面板只在相邻边保留分隔。文字不随背景一起变淡，页面也不通过整体透明度制造“轻”。

### 4.2 字体与信息密度

原 App 使用 IBM Plex Sans 的变量字体，官网内嵌的 App 演示也使用 IBM Plex Sans。设计基线采用易读无衬线正文，优先 IBM Plex Sans；中文使用系统回退字体。字体资源随应用本地加载，不让离线正文依赖网络字体。

建议字体栈：`"IBM Plex Sans", -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif`。计时和技术性短标记使用本地等宽字体或等宽数字。当前骨架声明的 Inter 只是现状；字体落实需在前端实施时处理。

| 角色 | 字号 / 行高 | 字重 | 使用规则 |
| --- | --- | --- | --- |
| 弹窗主标题 | 18 / 24px | 600～700 | 不作为每个任务卡的标题 |
| 计划区标题 | 16 / 20px | 700 | 英文可大写；中文正常排版 |
| 块标题、分区标题 | 14 / 20px | 600～700 | 与任务正文同尺度，通过字重区分 |
| 任务与会话正文 | 14 / 22px | 400 | 长标题自然换行，不固定一行裁切 |
| 次要正文、菜单 | 13 / 20px | 400～500 | 保持可读，不用低透明度替代文字层级 |
| 日期、状态、快捷键 | 12～13 / 16～18px | 400～500 | 含关键日期时不能只留截断文本 |
| 运行计时 | 16 / 20px | 600 | 数字宽度稳定，更新时布局不抖动 |

这些是对原 App 紧凑字级的整理，部分行高为中文阅读调整。等宽数字用于稳定计时读数，普通任务和会话保持无衬线正文。

### 4.3 间距、边界和图标

| 项目 | 基线 |
| --- | --- |
| 间距 | 4px 主节奏、2px 微调；常用 4 / 8 / 12 / 16 / 24 / 32px |
| 任务行 | 按内容自然增高，单行约 30～32px；控件贴近第一行基线，不随多行正文垂直居中 |
| 步骤缩进 | 每层约 20px；这是同周期步骤，不表示跨周期目标归属 |
| 面板/卡片 | 1px 细边界；白底计划面仅保留必要分隔，灰底计划面保留围框；两者几何一致，微型控件允许 2～4px 圆角 |
| 主按钮 | 高约 36px；紧凑按钮 28～32px；状态变化不挤动相邻内容 |
| 图标按钮 | 可见图形 16～20px，常用操作命中区建议至少 28×28px |
| 完成框 | 可见方框约 15～16px，扩大点击区；选中有勾选标记和语义状态 |
| 关系色槽 | 原始参考 6×13px、3px 圆角；可见尺寸与操作命中区分开 |
| 阴影 | 普通计划面默认不用；浮层仅在区分背景有必要时使用弱阴影，不制定全面禁令 |
| 图标语言 | 细线/简洁几何，风格接近原 App 的 Material Symbols；同类操作保持一致，并有名称 |

实现按照组件用途和内容密度落实间距，提取资源中的类名只作为取证线索。

### 4.4 主题切换

**用户确认保留两个主题：白底、灰底。** 两者都是浅色主题，复用相同的窗口栏、布局、组件和行为。首版默认白底是当前建议，用户选择后以其偏好为准。

1. 正式 App 在“设置 → 外观 → 主题”提供带小预览的白底、灰底选项；选中立即生效。看稿页面保留顶部开关，便于对照。
2. 主题切换只改变表面、边界和必要的文字色。字体、内容宽度、间距、标题与任务坐标保持一致，避免切换时任务跳动或换行。
3. 当前周期、草稿、光标、滚动位置、侧栏、关系选择和执行状态保持；不重建工作台。关系线仍贴合相同条目，目标颜色与状态含义不变。
4. 正式 App 的选择通过既有设置边界保存为非敏感偏好，启动时恢复；沿用 `app_settings`，不新增主题实体或数据表。设置契约在实施时补齐，避免恢复前明显闪成另一主题。
5. 预览仅用浏览器本地存储记住主题；存储不可用时仍可切换并说明无法保存。它不等于 App 的持久化实现。
6. 两个主题分别检查正文、提示、焦点、完成框、关系与提议叠加状态。系统深色偏好不自动改写这两个浅色主题的选择。

验收：切换前后内容几何及工作状态一致；窗口栏、画布、侧栏和浮层一起响应；重新打开后恢复选择。具体主题名和默认值可再调整，双主题能力已经确认。

预览验证：双主题切换前后的标题与任务几何一致，保留滚动、关系选择和侧栏；刷新恢复主题，浏览器存储失败时仍可切换并提示。灰底上的提示文字对比度为 4.78，灰色 hover 上的次文字为 4.53；390px 看稿宽度下侧栏完整、页面无横向溢出。这些是预览验证，正式 App 的草稿、执行和原生窗口状态仍需实现验收。

## 5. 任务编辑与条目状态

对应 UXR-006～013、031～033。

### 5.1 任务条目的构成

```text
[拖动入口] [完成框] [目标色槽] 任务标题              [反馈 / 收纳 / 更多]
                                └─ 同周期子步骤
```

任务标题占据主要宽度；行内操作在 hover、键盘进入条目或菜单打开时显露。已有 AI 提议的处理入口必须可发现，不能完全依赖 hover。保留一条可继续输入的空行，但空行不计入任务数量、复制范围或关系图。

| 输入/操作 | 结果 |
| --- | --- |
| 点击标题 | 定位光标并编辑；按 UXR-033 同时选中该条目的关系查看 |
| Enter | 在编辑上下文新建下一行；中文输入法组合期间只确认候选 |
| Tab / Shift+Tab | 在任务编辑器内调整步骤层级；菜单和表单内仍使用各自的焦点语义 |
| 切换周期/关闭面板 | 已提交内容保留；未提交内容不能因视图卸载静默丢失 |
| 勾选完成 | 只改变完成状态，不顺带切换关系目标或触发拖动 |
| 修改上层目标 | 在条目菜单中明确选择兼容目标，显示名称；内部子步骤保持 |
| 移到 Later | 明确反馈移动对象和撤销入口；关系兼容性按新容器重新判断 |
| 拖拽 | 从明确把手开始；正文选择、完成框、按钮、输入框排除；落点说明移动还是复制 |

不能把同周期步骤和跨周期链接合并成一种“父子树”。周任务链接长期目标后仍在周计划里；它自己的步骤继续缩进在周任务之下。

### 5.2 状态组合

任务的编辑、完成、关系查看和 AI 提议可以同时存在，不能用一个互斥 `status` 丢掉其他状态。这里只描述需要表达的信息，不要求按表新增领域字段。

| 状态 | 表达 | 与其他状态的关系 |
| --- | --- | --- |
| 普通 | 深色标题、完成框、可选色标 | 不铺满高亮 |
| hover / 键盘关注 | 轻背景、行内动作可见 | 不替代编辑光标或已点击的关系对象 |
| 编辑中 | 光标、清楚的输入位置 | 数据刷新不能重置选区或输入法组合 |
| 完成 | 勾选 + 删除线 + 仍可读的文字 | 完成不代表不能查看关系或编辑 |
| 关系选中 | 关系色的轻高亮、连线、退出入口 | 不抢占完成或提议的状态语义 |
| 提议新增/修改 | 待确认底色、标记、保留/撤销 | 关系查看叠加时，保留提议标记及动作 |
| 提议删除 | 危险浅底、删除提议说明、可撤销 | 确认前内容仍可查看 |
| 拖动中 | 原位置与落点可辨，拖动副本适度透明 | 暂时收起关系线，结束后重算；保留原点击选择 |
| 保存失败 | 行级失败说明与重试，保留输入 | 不假装已保存，也不清空用户文字 |

拖拽只负责表达操作与落点。跨列移动后的关联、复制来源和撤销恢复范围遵循 Q02/Q03；不能通过一个流畅动画掩盖尚未定义的行为。

## 6. 关联关系展示

对应 UXR-007～010、033。**点击后持续查看由用户确认**；直接关系范围和下面的操作裁决是当前设计基线。

### 6.1 查看与编辑关系分开

点击标题、条目非控件区域或“查看关联”入口，选中该目标/任务并展示关系。把“关联到上层目标”放在明确的条目操作中；查看关系不应顺带打开选择器或修改真实链接。

可见目标色槽用于快速识别和查看关联；菜单中提供“关联到上层目标 / 更换上层目标 / 解除关联”。没有关系时保留可发现入口和说明，不要求用户先选颜色才能开始记录。

默认展示当前上下文中兼容的**直接上层和直接下层**。选中周任务时可同时看到长期目标与日任务；不默认递归展开全部后代，也不凭标题相似补画日任务到专注块的边。

### 6.2 状态切换

| 当前状态 / 动作 | 界面结果 |
| --- | --- |
| 没有点击选择，悬停关系入口 | 临时显示该项直接关系，移开后收起 |
| 点击目标或任务 | 持续显示该项关系，移开指针仍保留 |
| 已有点击选择，指针经过别项 | 保留点击对象；不来回切换连线 |
| 点击另一条目 | 切换选择，重新计算可见关系 |
| 在同一标题中再次定位光标 | 保持关系查看，继续编辑，不反复开关 |
| 点击完成框/菜单/拖动把手 | 执行对应操作；不把事件冒泡成新的关系选择 |
| 显式退出或可用的 Escape | 收起关系高亮与连线，真实链接不变 |
| 切周期后所选项不再属于兼容上下文 | 清除查看选择；不改用户数据 |
| 所选项没有有效关系 | 就地说明未关联，提供关联操作；无假线 |
| 目标在屏外 | 显示名称和定位入口；滚动后仍能认出当前选择 |

Escape 的顺序：输入法组合 → 当前菜单/弹层 → 关系查看。不要让退出连线吞掉上层输入或弹层自己的取消动作。查看状态只需保存在当前工作区，不要求跨重启保存。

### 6.3 连线、色标和高亮

原 App 与官网中的 App 关系展示使用浅色细线、方形端点和行前窄色槽。**线与方形端点同色；深色描边属于行前色槽**。

![官网中展示的 App 目标关联](/Users/lordcasser/workspace/projects/goal/analysis/evidence/website-review-2026-09-14/connected-goals-ZoxmkM_E.png)

来源：W03。提取色槽、连线和任务组高亮；配图中的错位排列和大幅淡化用于突出示例。实际工作台保持稳定列位置，无关内容仍可读、可编辑。

| 元素 | 设计规则 |
| --- | --- |
| 归属色 | 长期目标的稳定信号，由有效下层链接继承；不把蓝色固定解释为工作、紫色固定解释为个人 |
| 行前色槽 | 浅填充与较深描边；查看时同时能读到目标名称，颜色不是唯一线索 |
| 连线 | 约 1 CSS px，S 形曲线，不随局部缩放明显变粗，不吞点击 |
| 端点 | 方形约 6×6px，与线同色；不用深色端点制造另一种含义 |
| 高亮 | 淡色覆盖相关条目及其步骤；未关联条目保持可读、可操作 |
| 无色目标 | 关系查看中使用中性色加名称是设计候选；具体默认分色规则仍见 Q05 |
| 状态叠加 | AI 提议底色和删除标记优先保留，关系可通过边线、色槽及连接表达 |

参考几何：父项右侧锚点 `(x1,y1)` 到子项左侧锚点 `(x2,y2)`，取 `xm=(x1+x2)/2`，用 `M x1,y1 C xm,y1 xm,y2 x2,y2`。y 锚定标题行，任务组高亮包含步骤；不要因展开更多步骤而把线滑到整组中部。

连线统一使用一个坐标系。主板滚动、列内滚动、面板开合、文字换行、字体载入和节点增删都要更新几何。端点不可见时不连到屏幕角落，也不跨过辅助面板制造错误位置。具体 SVG 层与定位方案在实现时决定，不沿用原 bundle 的 DOM 身份结构作为约束。

## 7. 专注块

对应 UXR-016～020、034。专注块服务于当前日，包含标题、计划时长和行动清单；一次集中推进不等于块中只能保存一条任务。

![原 App 的日计划与运行中的专注块](/Users/lordcasser/workspace/projects/goal/analysis/evidence/ux-audit-2026-09-14/11-focus-running.jpg)

来源：原 App O11。运行时间使用暖色，日计划与执行列表相邻；其他块的启动受运行状态约束。

| 状态 | 展示与可用操作 |
| --- | --- |
| 尚未建立 | 解释时间块用途，提供创建入口 |
| 计划中 | 标题、可编辑时长、行动内容、Start、更多；重复信息用图标和文字辅助识别 |
| 启动过渡 | 清楚显示倒计时或进入执行状态，避免重复启动；原 App 是 3 秒，具体值可调整 |
| 运行中 | Stop、稳定的剩余时间、进度和本次行动；同日其他块说明为何暂不能启动 |
| 已结束 | 显示实际/计划时长，允许收起或查看行动；Stop 表达结束，不暗示暂停后可继续 |

计划时长、当前剩余时间、全日实际投入使用不同名称。执行不足一分钟也可能有真实记录，不能根据界面显示 0 分钟判断其未启动。包含执行历史的周期删除守卫需要说明原因。

重复菜单只表达日程复用，不共享每次任务完成态或实际时长。修改未来实例、单日例外及停止范围按 Q07；日任务进入块是移动、复制还是引用按 Q04。暂停、完整复盘和日历排程不由本节默认加入。

## 8. 反馈、AI 与待确认改动

对应 UXR-021～030、035。

### 8.1 两条入口

![官网中展示的 App 反馈与助手界面](/Users/lordcasser/workspace/projects/goal/analysis/evidence/website-review-2026-09-14/second-opinion-CwaH4xDf.png)

来源：W04。用于核对问题摘要、处理入口与助手消息的表达；两块界面的配图拼接不规定应用必须同时打开两个侧栏。

| 入口 | 界面目标 |
| --- | --- |
| 随写反馈 | 先指出具体条目缺什么；用户可以直接修改，也可忽略或进入助手 |
| 主动规划/澄清 | 从当前周期或任务开始，明确上下文，在对话中逐步处理问题 |

问题入口采用计数或就地提示，不用模态打断写作。问题详情包含“涉及对象、具体发现、可采取动作”；允许带着问题继续计划和执行。结构检查与语义检查分别处理可用性，不能把 AI 出错显示成没有问题。

### 8.2 侧栏结构与会话状态

侧栏由标题及关闭、上下文说明、可滚动消息区、固定底部输入区组成。用户消息靠右并有轻底色；助手正文靠左；技能或工具状态使用简短状态行。当前问题比状态行醒目。

| 状态 | 设计规则 |
| --- | --- |
| 空会话 | 说明本次针对哪个计划，给一个可开始的入口；保留自由输入 |
| 输入中 | 自动增高到合理上限，之后内部滚动；不挤走整个消息区 |
| 处理中 | 显示简短进度，避免重复提交；状态播报不逐字刷屏 |
| 流式回复 | 用户停留底部时跟随；主动向上阅读后不强制拉回，提供回到最新消息入口 |
| 候选回答 | 可点击并显示快捷键；没有候选也可自由回答，不造空按钮 |
| 等待处理提议 | 提议在工作台可定位；会话说明变化，不以聊天气泡冒充已确认计划 |
| 失败 | 保留输入与历史，说明能做什么；地区/接入问题给配置路径，临时错误才提供合理重试 |
| 接入不可用 | 明确当前接入与恢复方式；本地编辑、链接、执行继续可用 |

官网 App 演示中的对话是预设内容；原 App 本轮成功 AI 回合及 Keep/Revert 未完成实测。这里描述目标体验，不能把演示回复当成供应商可用证明。

### 8.3 提议确认

1. 任务上的新增、修改、删除提议有可辨认状态与局部保留/撤销入口；查看修改前后内容时不丢失任务上下文。
2. 工作台底部汇总待处理数量；批量动作明确作用于当前周期还是全部受影响周期，不能靠侧栏位置猜范围。
3. 关闭侧栏、切换周期或再次打开应用不能隐式确认提议。列表变化后，计数与实际待处理内容一致。
4. 用户正在编辑时，异步提议或刷新不覆盖未提交草稿。冲突提示保留双方内容和可选择动作；后端如何协调另见 A02。

任务提议以外的澄清信息、优先级结论等确认范围仍受 Q08 约束。不要把本文扩大成“每次 AI 调用都必须开同一种确认弹窗”。

## 9. 空状态、引导、设置与浮层

### 9.1 空状态和引导

| 场景 | 应展示的内容 |
| --- | --- |
| 尚无某层周期 | 简洁图标、用途标题、一句说明、创建入口；依赖未满足时说明下一步 |
| 周期已有、任务为空 | 可直接输入的空行，可选规划入口；不重复要求创建周期 |
| 没有上层候选 | 说明当前上下文没有可关联项，保留未链接状态 |
| 没有专注块 | 解释用途与创建入口，而不是一张无法编辑的示例卡 |
| Later 为空 | 可直接记录；用途提示可以关闭，不要求填日期 |

虚线边界用于“这里尚未建立内容”或教学卡，普通错误不自动套用虚线。教学说明与真实任务分开；示例不参与统计、关系或 AI 请求。引导进度记录用户已体验的行为；关闭/跳过得到尊重，当前计划健康由反馈另行表达（Q06）。

### 9.2 弹窗与菜单

| 类型 | 规则 |
| --- | --- |
| 周期时长选择 | 选项与日期后果同屏；切换立即更新开始、推进、结束日期；取消不产生周期 |
| 删除影响确认 | 显示对象及影响数量，明确保护规则；取消优先聚焦，删除用具体名称 |
| 条目/周期菜单 | 靠近触发点，避免超出窗口；方向键可用，关闭回到触发入口 |
| 轻量提示 | 就近解释控件或禁用原因；关键限制不能仅在短暂 tooltip 中出现 |
| 成功/撤销提示 | 命名本次对象与动作；可操作时间及恢复范围明确，不打断持续输入 |

原 App 的 1/3/6 个月选择实际预览为 4/12/24 周。若保留这些标签，必须同时展示真实日期，不从标签名推算结束日。

浮层按“内容 → 关系线 → 辅助面板/工具栏 → 菜单 → 模态”管理层级；模态内的菜单属于当前模态。最终使用哪一种定位或原生 API 由实现验证决定，不能把原报告中的 vendor 样式推断当作平台限制。

### 9.3 AI 设置与配套入口

AI 设置先说明当前接入、可用性和费用来源，再提供连接或切换操作；区分本地保存和第三方 AI 发送。试用天数、供应商名称与商业模式是可调整内容，不能固定进通用组件。密钥不出现在普通状态、日志或错误提示中。

语音入口若实现，需要分别表示准备、采集中、处理中和失败；停止采集与提交文字不是一个含混动作。设置、更新、支持入口应能返回原计划。语音权限、通知、反馈发送等未实测路径继续按配套需求单独验收。

## 10. 动效、键盘和可读性

| 项目 | 首版设计基线 |
| --- | --- |
| hover / 轻状态变化 | 0～120ms；无需为每次状态切换附加缩放 |
| 面板与菜单 | 150～200ms 的短位移/透明度过渡；布局让位与动效一致 |
| 弹窗 | 约 160～220ms；关闭立即释放不可见拦截区 |
| 拖拽收尾 | 约 150～200ms；有清楚落点，取消后恢复原位置 |
| AI 更新 | 先靠状态标记与内容差异；不依赖弹跳让人发现改变 |
| 减少动效 | 去掉位移、缩放、脉冲和重复装饰动画，用静态状态文字表达进度 |
| 计时 | 数字更新不触发布局跳动；不逐秒向屏幕阅读器播报 |

所有主要动作有键盘路径。任务编辑、菜单、会话和全局快捷键按作用域分配；`⌘⇧L` 可沿用为 Later 开关，候选回复显示实际可用的 `⌘1…`，不能在无候选时拦截这些组合。

焦点与鼠标 hover 都能显露行内动作。图标按钮有名称，选中/展开/忙碌状态有语义；弹层关闭恢复焦点。正文换行、中文输入法、长日期与系统字号变化纳入验收。必要信息的对比度以实际前景、背景和透明度计算，不直接使用提取表中的字面值作为最终合格证明。

## 11. 前端状态与实现边界

当前仓库只有 React/TypeScript/Tauri 的占位界面。这份设计不要求迁回原 App 的 SolidJS，也不指定必须使用原编辑器、拖拽库或浮层库。实现代码原创，原 App 与官网产物只作为证据。

| 信息 | 所有权/前端处理 |
| --- | --- |
| 周期、任务、链接、执行记录、提议 | 后端是持久化事实来源；沿用既有 IPC 和事件边界 |
| 正在输入的文字、选区、输入法组合 | 编辑器本地暂存，明确提交与失败状态；刷新不强制替换 |
| 当前所选周期、关系查看对象、面板开关 | 工作台的界面状态；默认无需新建领域实体或数据库表 |
| 连线坐标、可见性、滚动位置 | 根据当前布局计算；不持久化为业务关系 |
| 审查/会话加载、可用性、错误 | 附带请求所属上下文；迟到结果不能写进另一个已切换的计划 |

组件按职责收敛为工作台外壳、时间导航、计划内容面、任务编辑器、关系展示、专注块、上下文面板和提议栏。Later 与计划复用编辑能力，问题与会话复用侧栏外壳；不要按每个视觉变体另造业务实体。具体目录仍以架构文档为准。

## 12. 验收场景与需求映射

下表用于下一轮出界面稿和实现验收，不表示当前骨架已通过。先在常见桌面内容区域（例如 1440×900、1280×800、1024×768）检查，再以长内容和面板组合检验边界；这些尺寸是测试样本，不是新产品的窗口下限。

| 画面/过程 | 必须检查 | 需求 |
| --- | --- | --- |
| 统一窗口栏与双主题 | 两套表面一致应用、切换无布局跳动且保留工作状态、保存选择、各自可读；安全区、拖窗、失焦/全屏/面板开合 | UXR-031/032；用户追加方向见 3.4、4.1、4.4 |
| 空工作台 → 创建周期 | 用途、主入口、日期预览、取消无副作用 | UXR-001～005 |
| 连续输入与子步骤 | Enter/Tab、中文组合、换行、切换后保存 | UXR-006/007/031 |
| 周/日关联 | 候选兼容、目标名称、稳定色标、未链接事务 | UXR-008/009 |
| 点击查看关联 | 移开保留、切换对象、退出、屏外定位、标题编辑、完成框和菜单互不误触 | UXR-010/033 |
| 关系与 AI 提议叠加 | 关系可辨，保留/撤销与删除提议仍可发现 | UXR-024/025/033 |
| Later → 安排 → 撤销 | 内容与步骤保留，关系失效有说明，撤销范围真实 | UXR-011～013 |
| 引导与提示 | 真实行为推进、教学不污染任务、可关闭与跳过 | UXR-014/015 |
| 执行三态 | 启动过渡、运行互斥、实际/预算、提前结束、删除守卫 | UXR-016～020 |
| AI 对话 | 上下文、当前问题、自由输入、候选、流式阅读、关闭后提议仍在 | UXR-021～025 |
| 反馈与失败 | 手动修改可处理问题、忽略作用域、AI 降级不锁本地功能 | UXR-026～029/035 |
| 规划收敛 | 有理由取舍，能识别下一步，允许保留未决定内容 | UXR-030/034 |
| 可读与可达 | 面板开合、长任务、纯键盘、减少动效、关侧栏/重启后的内容连续 | UXR-031/032 |

## 13. 待决策与当前骨架差异

| 问题 | 对前端的影响 | 后续处理 |
| --- | --- | --- |
| Q01 辅助面板与工作台空间 | 窄窗口下让位、折叠与滚动 | 按本文件基线出稿，再验证组合尺寸 |
| Q02/Q03 移动、撤销、复制防重 | 成功提示、撤销承诺、重复复制反馈 | 先选行为，再落实交互；不由前端单独假定恢复完成 |
| Q04 日任务进入专注块 | 完成状态是否联动、是否显示关系线 | 在复制/移动/引用之间明确选择后完善执行衔接 |
| Q05 颜色与无色关系 | 默认分色、中性关系可见性 | 保留名称路径；中性线方案仍属自有候选 |
| Q06/Q07 引导与重复 | 完成记录及未来修改范围 | 与既有记录策略核对，避免反复教学或改写历史 |
| Q09 周期与历史边界 | 日期标签、剩余时间、历史操作的可用状态 | 明确自然时间和周期边界后统一显示与禁用说明 |
| Q08/Q10/Q11 AI 范围与反馈 | 批量动作对象、重试、忽略范围 | 明确契约后落实文案和状态，不从演示猜范围 |
| Q12 点击关系与输入可达性 | 编辑、选择、键盘、Escape 优先级 | 按用户确认的点击能力验证完整闭环 |
| A01/A02 删除语义与异步重叠 | 删除预览是否真实、草稿是否被覆盖 | 架构债务单独处理，不混入本次文档整理 |

当前 [src/index.css](src/index.css) 已有灰阶、暖色和 40px 顶栏；重做时按 3.4 调整为整合后的 52px 窗口栏，并核对原生控件安全区。[窗口配置](src-tauri/tauri.conf.json) 当前使用 `Transparent` 与 `hiddenTitle`，这只是骨架配置，尚未验证能否满足整合布局。具体窗口 API 和定位方案在实施时核对。

当前骨架还缺关系色、提议状态与完整组件使用规范；样式中“只有一个强调色、无阴影”的注释不能扩大为关系色或浮层的全局禁令。[src/App.tsx](src/App.tsx) 仍是连通性占位内容，不是本设计的完成态。既有 OpenSpec 中“所有层一屏同时可见”“连线端点必须更深”“跨层链接决定缩进”等冲突已记录在复核材料中，实施前按选定需求分别同步。

后续修改本文件时，行为变化回到对应 UXR/Q；颜色、尺寸、布局和组件状态在此收敛；技术边界回到架构文档。保持一份前端设计入口，避免再次把原始观测值、产品需求与自有实现方案混写。
