# Planning cycle controls GUI evidence — 2026-09-16

验收范围：仅使用 macOS 原生 GUI/CUA 操作，未修改生产代码、未写入数据库、未配置或保存真实 AI provider。

## 包与状态

- UI UX Pro Max 复核后已生成新的签名验收包：`/Users/lordcasser/workspace/projects/goal/.artifacts/desktop/uiux-review/Goal.app`。此前三份本地中间验收包已被替代并清理，Rust 缓存清理释放约 6.5 GiB。下列功能验收保留当时的实际包与结果；最新视觉复验另见 `ui-ux-pro-max-gui-2026-09-16.md`。
- 退出了旧的 `headers-review` / 中间 `planning-review` 实例后，启动并复验了签名包：`/Users/lordcasser/workspace/projects/goal/.artifacts/desktop/planning-review-final/Goal.app`。
- 期间创建的临时周期、父任务和子任务均已通过 GUI 删除；最终仅保留原有计划数据。
- 最终语言已恢复为 `zh-CN`。最终界面按要求保留在“长期周期 → 自定义”弹窗，默认日期草稿未创建。

## 长周期与自定义检查

- `zh-CN` 下观察到 1/3/6 个月预设均为中文，包含“1 个月 4 周”“3 个月 12 周”“6 个月 24 周”及中文的目标、进度检查、复盘预览。
- 自定义模式可编辑开始/结束日期和重复检查；将结束日期设为早于开始日期时，界面显示“结束日期必须晚于开始日期”“请输入有效的日期范围”，且“创建周期”按钮 disabled。
- 在 Repeat 与 Once 两种模式之间切换后，Repeat 草稿（2 周）仍被保留，未保存 Once 草稿。
- 为验证真实持久化，GUI 创建了临时周期 `2037-02-28 → 2037-03-17`，重复每 2 周；重新展开“进度检查安排”观察到“每 2 周检查”和 `3月14日`。
- 原生日期控件通过步进器完成了上述日期输入。Custom 的“天/周”下拉在 CUA 原生列表中可打开和选择，但该列表未能稳定收起，未反复耗时；真实 fixture 使用了已验证的“每 2 周”。因此未将“每 2 天”作为通过项。

## 递归任务删除

- 在上述临时周期中创建了“GUI验收临时父任务”和嵌套的“GUI验收临时子任务”。
- 父任务删除确认对话显示：当前任务及 `1` 项关联子目标/子任务，并说明关联子项会逐级删除。
- 点击“取消”后父、子任务仍在；再次确认删除后父、子任务均消失。

## 周期删除

- 临时周期删除预览显示周期内 `1 项任务`、`0 个下级计划`、`0 个专注块`，并说明其他计划中的关联子项及提醒也会逐级删除。
- 点击“取消”后周期仍在；再次确认“删除长期周期”后临时周期从周期栏消失。

## 滚轮与滚动条

- CUA 当前仅提供按方向/页数的滚动调用，无法注入真实鼠标 wheel/trackpad delta。主面板中也未观察到可供该调用可靠改变的横向溢出内容；尝试的一次滚动前后界面无可判定变化。
- 因此没有把“默认横向、点击纵向 overflow 后纵向、点击外部恢复、细 scrollbar”写成 GUI 通过项，也没有声称完成实机鼠标验收。

## AI 设置空态与添加表单

- final 包中文空态：`供应商`行不再显示灰色长条；“添加供应商”按钮位于“管理供应商与模型”标题旁，空态说明正常显示。
- final 包英文空态同样观察到 `Add provider` 与 `Manage providers and models` 并排，且没有灰色空条。
- 中英文“添加供应商”表单均已打开观察。窄窗口下表单为紧凑单列布局，包含名称、Base URL、API Key、API format、可选请求 Header、模型区域及 disabled 的保存按钮；未输入或保存任何 provider。

## 结论

已完成的 GUI 事实仅限于上述直接观察项。滚轮真实设备行为、每 2 天单位持久化，以及截图文件路径均未冒充通过或生成。
