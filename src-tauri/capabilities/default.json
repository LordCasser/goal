{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability for the Goal app",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:allow-reveal-item-in-dir",
    "dialog:allow-save",
    "allow-planner-commands"
  ]
}
