{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-window",
  "description": "macOS-only native window drag capability for the main window",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}
