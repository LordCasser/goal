{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-window",
  "description": "macOS-only native window drag capability for the Goal main window",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    "core:window:allow-start-dragging"
  ]
}
